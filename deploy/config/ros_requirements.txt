# ============================================================
# ROS MES 机器人控制后端 Python 依赖
# 安装方式：pip install -r requirements.txt
# 注意：需先安装 ROS Noetic/Melodic 的 rospy 环境
# ============================================================

# ---- 核心科学计算 ----
numpy>=1.21.0
scipy>=1.7.0

# ---- 点云处理 ----
open3d>=0.15.0

# ---- 串口通信（V1.1 主链路已迁移至 CAN；serial_test_node 等调试工具仍在使用，保留）----
pyserial>=3.5

# ---- CAN 总线（V1.1 起为 hardware_node 必需依赖，CAN 2.0B 29位扩展帧）----
python-can>=4.0.0

# ---- Web 服务（data_process_node 内嵌 Flask）----
flask>=2.0.0

# ---- 数据库 ----
# sqlite3 为 Python 标准库，无需安装

# ---- rosbridge 相关 ----
# rosbridge_suite 通过 apt 安装，不是 pip 包
# websockets 用于后端与 rosbridge 通信
websockets>=12.0

# ---- 其他工具 ----
python-dotenv>=1.0.0
